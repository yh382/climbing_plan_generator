export const cmToIn = (cm?: number | null) => (cm ?? 0) / 2.54;
export const kgToLb = (kg?: number | null) => (kg ?? 0) * 2.20462262185;
export const inToCm = (inch?: number | null) => (inch ?? 0) * 2.54;
export const lbToKg = (lb?: number | null) => (lb ?? 0) / 2.20462262185;
